<h2>Edit This Movie Details</h2>

{{#with allMovies}}

<form action="/movies/{{_id}}/edit" method="POST">
  <label for="title">Title: </label>
  <input type="text" name="title" id="title" value="{{title}}">

  <label for="genre">Genre: </label>
  <input type="text" name="genre" id="genre" value="{{genre}}">

  <label for="plot">Plot: </label>
  <input type="text" name="plot" id="plot" value="{{plot}}">

     {{!-- <label for="cast">Cast: 
     <select name="cast" id="cast" multiple size="3">
    {{#each cast}}
      <option value="{{_id}}">{{name}}</option>
    {{/each}}
  </select>
  </label> --}}

    {{!-- <label for="cast">Cast: 
     <select name="cast" id="" multiple size="3">
    {{#each allCelebrities}}
      <option value="{{this._id}}">{{this.name}}</option>
    {{/each}}
  </select>

   <label for="cast">Cast: </label>
    {{#each cast}}
  <input type="text" name="cast" id="cast" value="{{name}}">
   {{/each}} --}}

  <button type="submit">Confirm this edit</button>
</form>

{{/with}}