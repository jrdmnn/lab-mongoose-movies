{{#with movie}}
<h2>Edit {{title}}'s information</h2>
<form action="/movies/{{_id}}" method="POST">
  <label for="title">Title: </label>
  <input type="text" name="title" id="title" value="{{title}}">

  <label for="genre">Genre: </label>
  <input type="text" name="genre" id="genre" value="{{genre}}">

  <label for="plot">Plot: </label>
  <input type="text" name="plot" id="plot" value="{{plot}}">
{{/with}}
{{#with celebritiesAll}}
  <label for="cast">Cast: </label>
  <select name="cast" id="cast" multiple>
    {{#each this}}
      <option value="{{_id}}">{{name}}</option>
    {{/each}}
  </select>

  <button type="submit">Update</button>
</form>
{{/with}}

{{!-- Create handlebars helper function: if celebritie is already in the cast I set it to "selected" by default --}}